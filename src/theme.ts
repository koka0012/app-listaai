import { DefaultTheme, Theme } from 'react-native-paper'

const theme: Theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#1c9dc1',
    accent: '#2d4ccb',
  },
}

export default theme
